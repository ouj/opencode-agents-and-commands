---
description: Run tests with coverage
agent: build
model: opencode/big-pickle
---

- If a test runner exists, run the full test suite with coverage and report any
  failures.
- If no test runner exists, say so and suggest validating changes by running
  the relevant OpenCode command(s).
